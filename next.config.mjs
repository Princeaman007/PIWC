/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/PIWC",
    output: "export",
    reactStrictMode: true,
    images: {
        unoptimized: true,
    },
};

export default nextConfig;